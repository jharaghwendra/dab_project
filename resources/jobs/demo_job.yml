resources:
  jobs:
    demo_job:
      name: demo_job
      tasks:
        - task_key: demo_task_1
          notebook_task:
            notebook_path: ../../citibike_etl/notebooks/01_bronze/01_bronze_citibike.ipynb
            source: WORKSPACE
          environment_key: Default
      queue:
        enabled: true
      environments:
        - environment_key: Default
          spec:
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED